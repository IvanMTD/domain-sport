create table if not exists ekp(
    id bigint GENERATED BY DEFAULT AS IDENTITY,
    num int,
    title text,
    beginning date,
    ending date,
    access text,
    category text,
    location text,
    ekp text,
    s float,
    d float
);